---
# gstack: design-md-format=spec
name: PropInspec
description: An industrial, restrained review tool where every hard number reads as exact and checkable, so a reviewer can trust it before it reaches a tenant or owner.
colors:
  primary: "#A9791F"          # mustard/ochre — the inspector's grease pencil; primary actions + "pending review"
  on-primary: "#FFFFFF"
  surface: "#FFFFFF"
  background: "#F6F7F6"
  surface-alt: "#EEF0EE"
  border: "#DDE1DD"
  text: "#14171A"
  text-muted: "#6B7280"
  accent: "#A9791F"
  accent-ink: "#5C4212"
  accent-bg: "#FBF1DD"
  success: "#2F7D5C"          # Good condition / Approved
  success-bg: "#E7F3ED"
  warning: "#A9791F"           # shares the accent — "pending review" IS the warning state, no separate hue
  fair: "#B45309"              # Fair condition — distinct from accent: more saturated, more orange
  fair-bg: "#FDF0E4"
  error: "#B3261E"             # Damaged condition
  error-bg: "#FBEAE8"
  neutral: "#8A8F98"           # Not Rated condition
  neutral-bg: "#EFEFF1"
  vendor: "#3D6B8A"            # Outside Vendor identification, Dispatch Board only — see Decisions Log 2026-09-18
  vendor-bg: "#E9F1F6"
  brand-cyan: "#29BFEB"        # GPM logo mark only — never used in app chrome
  brand-indigo: "#3B2E8C"      # GPM logo mark only — never used in app chrome
typography:
  display:
    fontFamily: "Cabinet Grotesk"
    fontWeight: 700
    fontSize: "clamp(1.75rem, 1.4rem + 1.4vw, 2.5rem)"
    letterSpacing: "-0.01em"
  body:
    fontFamily: "General Sans"
    fontSize: 1rem
    lineHeight: 1.5
  label:
    fontFamily: "General Sans"
    fontSize: 0.75rem
    letterSpacing: 0.04em
  mono:
    fontFamily: "JetBrains Mono"
    fontFeature: tnum
rounded:
  sm: 3px
  md: 6px
  lg: 8px
  full: 9999px
spacing:
  xs: 4px
  sm: 8px
  md: 16px
  lg: 24px
  xl: 32px
  2xl: 48px
components:
  button-primary:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.on-primary}"
    rounded: "{rounded.sm}"
  button-primary-hover:
    backgroundColor: "#8F6519"
  button-secondary:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.text}"
    borderColor: "{colors.border}"
    rounded: "{rounded.sm}"
  input:
    borderColor: "{colors.border}"
    rounded: "{rounded.sm}"
  table-row-hover:
    backgroundColor: "{colors.surface-alt}"
  badge-pending-review:
    backgroundColor: "{colors.accent-bg}"
    textColor: "{colors.accent-ink}"
  badge-reviewed:
    backgroundColor: "{colors.success-bg}"
    textColor: "{colors.success}"
  badge-exported:
    backgroundColor: "{colors.neutral-bg}"
    textColor: "{colors.neutral}"
  condition-fair:
    backgroundColor: "{colors.fair-bg}"
    textColor: "{colors.fair}"
  card:
    backgroundColor: "{colors.surface}"
    rounded: "{rounded.lg}"
  nav-link:
    textColor: "{colors.text-muted}"
---

# PropInspec

## Overview

**Creative North Star:** An industrial, function-first review surface where every hard number is set in mono so it reads as exact, unedited, and checkable — the opposite feeling of a marketing dashboard.

**Product context:** PropInspec is GPM Property Management's internal tool for reviewing AI-generated move-out inspection reports before they reach a tenant or owner. Reviewers: Chuck (field inspector), Jessica Zilka (maintenance supervisor), Courtney (quoting). The AI extraction pipeline produces structured reports from inspection video; this dashboard is the required human review gate — nothing goes out unreviewed. GPM is the brand; PropInspec is "powered by" — the logo lockup always leads with the GPM mark.

**Mode per surface:** Inspections list = Operate (dense triage table). Inspection detail/review = Operate (structured data + photo evidence, one review decision per page). Cost Book = Operate (a working ledger, not a report).

**Reference sites:** Researched Stripe's and Linear's dense-table patterns (internal ops tools tolerate density; users are power users exploring, not first-time visitors) and the property-management inspection software landscape (zInspector/AppFolio) for category baseline, 2026-09-10.

**Key characteristics:**
- GPM logo is the primary brand anchor everywhere; "Powered by PropInspec" is a small secondary lockup, never the headline
- Dense data table as the default surface, not card mosaics
- Every measurement, GPS coordinate, timestamp, and dollar amount renders in tabular-figure mono
- Condition taxonomy (Good / Fair / Damaged / Not Rated — the actual `line_items.condition` enum) drives the only four semantic colors in the system
- Light mode only: this is reviewed at a desk during business hours

## Colors

**Strategy:** Restrained — one functional accent (ochre) plus neutrals, with a single scoped exception (`vendor`, added 2026-09-18 for the Dispatch Board — see Decisions Log); GPM's brand cyan/indigo are reserved for the logo mark itself and never bleed into app chrome, buttons, or data visualization. Keeping the two systems separate means a GPM rebrand or a new "Powered by" partner never forces a rework of the working UI.

**Light or dark:** Light only. This is reviewed at a desk, mid-shift, matching real paperwork and a well-lit office — not a developer tool used late at night.

Named rules: `accent` (ochre) carries both primary interactive actions AND the `pending_review` status badge — deliberately doubled, since "pending review" IS a call to action ("go look at this"). `success`/`fair`/`error`/`neutral` map 1:1 to the real `line_items.condition` enum (Good/Fair/Damaged/Not Rated) — never invent a fifth condition color, and never reuse these four for anything else. `fair` is a distinct saturated orange, not a lighter version of `accent` — the two must stay visually separable since both can appear on the same row. Neutrals (`background`, `surface`, `surface-alt`, `border`) are all cool slate-grays derived from the same hue as `text`, never mixed warm/cool. `vendor` (a muted slate-blue, distinct from both `brand-cyan`/`brand-indigo` and every condition color) marks an Outside Vendor's cards on the Dispatch Board specifically — it is scoped to that one screen's crew-vs-GPM-Staff distinction and is not a general-purpose second accent; GPM Staff cards there stay in the plain neutral treatment every other card in the app uses.

## Typography

Cabinet Grotesk and General Sans (Fontshare, same foundry — coheres as a family) cover the interface: Cabinet Grotesk's slightly architectural, precise geometry carries headings and nav labels; General Sans, built for UI, carries body copy and dense table cells at small sizes without going mushy. JetBrains Mono (Google Fonts) carries every hard number — this is the one deliberate typographic risk in the system: measurements, GPS coordinates, timestamps, and dollar amounts are ALWAYS mono with tabular figures, even inline in a sentence, so a reviewer's eye learns to trust mono text as "this came straight from the source, unedited."

Loading: `<link>` to Fontshare's CSS API for Cabinet Grotesk (700/500) + General Sans (400/500/600); Google Fonts `<link>` for JetBrains Mono (400/500/700).

Scale: display uses a fluid clamp so page titles don't reflow awkwardly between the table view and the detail view; body stays fixed at 16px (never smaller, per accessibility floor); labels stay at 12px uppercase with tracking for scannability in table headers.

## Layout

Grid-disciplined. Two-column app shell: a narrow (200px) nav rail and a flexible main content area — no dashboard-card mosaic. The inspections list is a single dense table, 36-44px rows, no zebra striping (hover state carries row emphasis instead). Detail/review views break the grid intentionally: a fixed-width evidence frame (280px) sits beside flowing review notes, because photo evidence needs a stable frame while notes vary in length.

## Elevation & Depth

Borders, not shadows. A 1px `border` token separates every surface (nav rail from content, table rows, cards, evidence frames). The one exception is the theme toggle / floating controls, which may use a small offset+blur shadow to read as "floating above" the page — everything else is flat. A line item's timeline bar mid-drag counts as a temporarily floating control under this same exception (see Decisions Log, 2026-09-11).

## Shapes

Two radii only: `sm` (3px) for buttons, inputs, and badges; `lg` (8px) for the app shell and evidence frames. No radius on table rows or dividers. Never a bubbly uniform radius across unrelated element types.

## Components

- **button-primary**: ochre fill, white text, `sm` radius. Hover darkens ~10%. This is the only solid-fill button — everything else is bordered or ghost.
- **badges** (pending_review/reviewed/exported): tinted background + matching text color, no border, small dot indicator. These map 1:1 to `inspections.status` — a separate vocabulary from the per-line-item Good/Fair/Damaged/Not Rated condition dots.
- **table rows**: no default background; `surface-alt` tint on hover only. Never disable hover — this is a scanning surface.
- **evidence frame**: photo + a mono caption bar baked into the frame (GPS + timestamp), border-only, no shadow. This is the one component built specifically to serve the "trustworthy evidence" north star — never reduce it to a plain unlabeled image.
- **inputs**: bordered, `sm` radius, mono font specifically for any field holding a measurement or coordinate.

## Do's and Don'ts

- Do: put every measurement, coordinate, timestamp, and dollar amount in mono with tabular figures, everywhere it appears — table cells, detail views, even inline text
- Do: lead every screen's brand lockup with the GPM logo; "Powered by PropInspec" stays secondary and small
- Do: keep the four condition colors (Good/Fair/Damaged/Not Rated) reserved exclusively for condition state — never reuse green/orange/red/gray for anything else
- Do: default every screen to light mode
- Don't: introduce GPM's brand cyan/indigo into buttons, links, charts, or any functional UI — they live in the logo mark only
- Don't: wrap the inspections list in dashboard cards or stat tiles — it's a table, not a summary view
- Don't: add a second general-purpose accent color; the ochre accent already does double duty (primary action + `pending_review` badge) on purpose. The one exception is `vendor` on the Dispatch Board (2026-09-18), scoped to that single screen's crew-type distinction — it doesn't license a new color anywhere else
- Don't: use a plain `<img>` lightbox for inspection photos — always the evidence-frame component with its mono caption

## Motion

- **Approach:** minimal-functional — motion only where it aids comprehension (a row expanding, a status changing), never decorative
- **Easing:** enter(ease-out) exit(ease-in) move(ease-in-out)
- **Duration:** micro(50-100ms) short(150-250ms) medium(250-400ms)
- **The one authored moment:** a status badge transition (pending_review → reviewed/exported) briefly scales and settles, so a reviewer's action registers as confirmed without a toast notification

## Decisions Log
| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-09-10 | Initial design system created | Created by /design-consultation: Industrial/Utilitarian direction, ochre accent, Good/Damaged/N/A semantic colors, Cabinet Grotesk + General Sans + JetBrains Mono, informed by Stripe/Linear dense-table research and the existing Zinspector report taxonomy |
| 2026-09-10 | GPM logo made primary brand anchor, product renamed PropInspecAI → PropInspec | User decision: GPM is the brand; PropInspec drops "AI" from its name and appears as a secondary "Powered by" lockup, never the headline |
| 2026-09-10 | Condition palette corrected to 4 states (added `fair`) | The real `line_items.condition` check constraint is Good/Fair/Damaged/Not Rated, not the 3-state Good/Damaged/N/A assumed from the old Zinspector report during the design pass — found while implementing the real inspections list against the actual schema |
| 2026-09-10 | Inspections list restructured: grouped by status, Condition column dropped, "Add New Inspection" added | User decision: the overview page is for triage-by-status, not a condition summary; condition detail belongs on the detail page |
| 2026-09-10 | Line-item edits are bulk-saved (one "Save all changes" button), not per-row | User decision: removed per-row Save buttons in favor of a single page-level submit — the review is one editing session, not N independent ones |
| 2026-09-10 | Outside-vendor assignment is a link-styled `<select>`, not a modal | Native select styled as an ochre underlined link satisfies "click to pick a vendor" without new client-side state; vendors table (ACME 1/2/3 seed) added |
| 2026-09-10 | `still_image_file` column added to line_items, hover-preview built ahead of the extraction pipeline | UI and data model ready for when the video-still-extraction step (see docs/designs) lands; renders "No still extracted yet" until then |
| 2026-09-10 | Export gets a real file: `/inspections/[id]/export` route generates an .xlsx via exceljs | Column set is a placeholder pending Courtney's actual rehab-quote template (still not obtained per the design doc's own Open Questions) |
| 2026-09-10 | Line-item table widened to fit without horizontal scroll (AppShell `wide` prop, dropped forced `min-w`) | User feedback: dense 8-column table shouldn't require side-scrolling on a normal desktop viewport |
| 2026-09-10 | Inspection status toggle: "Mark Exported" button becomes "Under Review" once exported, reverting status to `pending_review` | User decision: reviewers need a way back from Exported without a separate admin action |
| 2026-09-10 | "Create Move Out Report" generates a PDF via pdfkit at `/inspections/[id]/move-out-report`, structured to match `Blank Move In Checklist.pdf` (GPM letterhead, Property/Tenant/Date/Type block, per-room Item/Status/Comments table, signature block) | Michigan law requires the move-out checklist to match the move-in checklist's structure (MCL 554.608 family). Two things deliberately NOT guessed: (1) the statutory notice paragraph — the move-in form's notice text is move-in-specific; move-out text differs and wasn't supplied, so it's a marked placeholder pending counsel review; (2) Tenant Names — `inspections` has no tenant_name column, left blank rather than fabricated. Condition display maps Damaged→"Poor" and Not Rated→blank to match the checklist's Good/Fair/Poor scale |
| 2026-09-10 | Still images are actually extracted from real video via ffmpeg (`scripts/extract-stills.mjs`, midpoint of `source_timestamp`) | Local `public/stills/` copy kept for dev convenience only (gitignored); the source of truth is Supabase Storage (see next row) |
| 2026-09-10 | Reversed: stills upload to a **public** Supabase Storage bucket (`inspection-stills`), `still_image_file` stores the public URL directly — works identically local and on Vercel, no signed URLs, no auth | User correction: these are GPM's own post-move-out evidence photos of a now-vacant unit, not restricted tenant data — the earlier "keep it gitignored/local-only" caution was miscalibrated to this case. `scripts/lib/supabase-admin.mjs` creates the bucket; `scripts/migrate-stills-to-storage.mjs` moved the first 8 already-extracted stills over. Needs `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` in `dashboard/.env.local` (local script use only, not read by the deployed app) |
| 2026-09-10 | Set Up page added (GPM Labor Charge $/hr, Material Markup %, Vendor Markup %), backed by a singleton `settings` table | User request. Only `gpm_labor_charge` is wired into a calculation so far (see next row); the two markup percentages are stored but not yet applied anywhere |
| 2026-09-10 | Labor on a line item is entered as **hours** (0.25 increments), not a dollar amount — `labor_cost = labor_hours * settings.gpm_labor_charge`, computed and stored at save time by `bulkUpdateLineItems` | User correction: the earlier "0.25 increments" spec was for hours, which I'd misread as a dollar step. Stored (not computed on read) so a later rate change doesn't silently alter past reports. `LaborHoursInput` (client component) shows a live `hours × rate` preview before save |
| 2026-09-10 | Move-Out Report's Itemized Charges table shows the `hours @ $rate/hr` breakdown under the Labor $ figure, not just the total | User request: "the charges ... needs to reflect the calculation" — an auditable breakdown, not just a number |
| 2026-09-10 | Fixed: inspection-detail line-item grid columns desynced from their headers | Root cause: `ROW_COLS` used bare `Nfr` grid tracks; since each row is an independent `display:grid` container, a row whose Item cell held a long unbroken string (the full Supabase Storage still-image URL) had its Item track's min-content forced wider than its fr share, pushing that row's other columns right relative to the header row (which has short text and stayed put). Fixed with `minmax(0,Nfr)` on every track plus `min-w-0` on each cell div (grid items default to `min-width:auto` independent of track sizing) and `break-all` on the still-image label. Also switched the label itself from the full URL to just the filename — more readable and a smaller belt-and-suspenders fix |
| 2026-09-10 | Every line-item field is now inline-editable in the bulk-save form — Room/Area, Item, Recommended Action, Observed Evidence, Source Timestamp as text inputs; Condition and Assigned To as selects | User request: "everything ... with exception of Image and Mp4 file name" — `still_image_file` (EvidenceStill) and `source_video_file` stay plain text/display, nothing else does |
| 2026-09-10 | Assigned To gained a third option, "Other" (`line_items_assigned_to_check` constraint updated) | User request. The vendor picker (`VendorSelect`) now only shows when the *currently stored* Assigned To is "Outside Vendor" — changing the dropdown and saving is a two-step reveal (no client JS reactivity added for this), same pattern as the Materials/Labor/Vendor-Est. disabled states |
| 2026-09-10 | Inspection status is edited via the status pill itself (`StatusSelect`, a client component, auto-submits `updateInspectionStatus` on change) — the separate "Mark Exported"/"Under Review" buttons are gone | User request. Replaces the two-state toggle with a direct 3-way select (Pending review / Reviewed / Exported); `StatusBadge`'s style map is now exported and shared so the pill and the read-only list-page badge stay visually identical |
| 2026-09-10 | "Duplicate Section" button, bottom-right of each line item, inserts a copy directly below it | `duplicateLineItem(id, inspectionId)` bound via `.bind()` as the button's `formAction` (NOT a `name`/`value` pair on the button — that collides with Next.js's own `$ACTION_ID_...` encoding for function-valued `formAction`s and produces a hydration-mismatch error). New row's `created_at` = original's + 1ms, so it sorts immediately after the original under the existing `order by room_area, created_at` |
| 2026-09-10 | "Tenant Status" renamed "Tenant Charge"; the Charge/Approved radios became two bare checkboxes with no text | `TenantChargeCheckboxes` (client component) renders two independent-looking `<input type="checkbox">`s but keeps them backed by one hidden input / one DB column (`tenant_status` still only holds `tenant_charge` \| `approved` \| null) — checking one clears the other in local state, submitted as a single value |
| 2026-09-10 | "Download Excel" replaced by "Create Turn Scope" — creates a **real Google Sheet** (not a downloaded file), matching the structure of GPM's `Turn Scope - 19946 Kinloch....xlsx` reference (bold-bordered Area/Details/Comments/Vendor-GPM/Hours/Stage table, running-hours-total formula, VendorsEstimates tab), and drops it directly into the matching property folder on GPM's shared Drive | User-provided a service-account key (`GOOGLE_SERVICE_ACCOUNT_JSON` env var, same pattern as `DATABASE_URL` — works locally and on Vercel; never read the local `google-service-account.json`/`gpm-propinspec-*.json` files directly, both gitignored). **Real bug hit and fixed:** `sheets.spreadsheets.create` always tries to create in the caller's own Drive space; modern service accounts have zero personal storage quota there and get a 403 ("The caller does not have permission") no matter what scopes/roles are granted. Fixed by creating the file directly inside the target Shared Drive folder via `drive.files.create({parents:[folderId]})`, then populating it with `spreadsheets.batchUpdate` (`updateCells`/`mergeCells`/`updateBorders`) instead. Property folders are matched by a drive-wide name search (`"<street name> <street number>"`, e.g. "Brest 1554") across both Shared Drives ("Property Files A-O" / "Property Files P-Z", further split by street-name letter range one level down — searched with `corpora:'drive'` and no parent filter so the letter-range nesting doesn't matter); a folder that isn't found returns a clear error rather than guessing a location. `lib/google.ts` holds the shared auth + folder-lookup helpers |
| 2026-09-10 | Video clips are now playable from the detail page: filename is a link opening a popup with a Drive preview `<iframe>` | Videos upload to Drive (`scripts/upload-videos.mjs`) into a dedicated "Inspection Videos" subfolder inside the property's folder — NOT Supabase Storage. **Real limit hit:** a real 162MB clip 413'd against Supabase Storage's project-wide default 50MB upload cap (a project setting, not something the app can raise); Drive has no comparable low default, so clips up to ~260MB uploaded fine. The subfolder keeps raw clips separate from the property folder's existing long-lived document archive (real work orders, water bills, tax records — confirmed by listing it). The popup embeds Drive's own `/preview` iframe rather than a direct `<video src>`: Drive doesn't serve raw bytes for that without making the file public, and unlike the stills (confirmed OK to be public — tenant moved out), full walkthrough video is left behind Drive's real permissions, so only a signed-in GPM Drive member can actually play it |
| 2026-09-10 | `source_timestamp` removed from the UI entirely (no longer editable, no longer touched by `bulkUpdateLineItems`'s UPDATE at all) | User request, superseded by the video popup — existing values are preserved untouched in the DB (just orphaned/unused) rather than nulled out by a save that no longer submits that field |
| 2026-09-10 | VendorSelect's placeholder option text removed ("— select vendor —" → blank) | User request. The option itself (empty value) stays — needed so an Outside-Vendor row with no vendor chosen yet still has a valid unselected state to save as null |
| 2026-09-10 | "Create New Vendor" added to Set Up (with the current vendor list shown above it) | User request — `vendors` had no UI to add to it beyond the 3 seeded placeholders |
| 2026-09-11 | Job Timeline (`/inspections/[id]/timeline`): new per-inspection view tracking `line_items.status`/`scheduled_start`/`scheduled_end`/`blocks_line_item_id` (rehab/turn job tracking, distinct from the review-document lifecycle in `inspections.status`). Job status and assignee type render as plain text, never color-coded — reusing condition/accent tokens for a different taxonomy was caught as a real violation during /plan-design-review and reverted before shipping. Blocked items get the loudest treatment on the page (a diagonal hatch in the existing `error` token) since that's the signal a reviewer needs first, not assignee type. Dependencies (`blocks_line_item_id`) show as a small "← blocked by: X" indicator (click scrolls to and highlights the predecessor row) rather than a drawn connector line — a line only has a defined look for the same-row/adjacent-time case, and real dependencies are routinely cross-room. A dragged timeline bar's shadow is a licensed extension of the existing floating-control shadow exception (see Elevation & Depth above) | Scoped via /office-hours → /plan-eng-review → /plan-design-review (2026-09-11, see `docs/designs/propinspec-job-tracking-scope.md`). Dates stay editable via plain `<input type="date">` fields alongside the drag interaction — keyboard-accessible by construction, not a bolted-on separate mode. Staff-only in v1 (no vendor login); the app currently has no auth system at all (tracked in TODOS.md, pre-existing, not introduced by this feature) |
| 2026-09-17 | Job Timeline regrouped from per-room to per-batch rows, still one row per line item | User request: line up Timeline with the Quote Sheet's new Batch View, since batches (not rooms) are the unit that gets scheduled/dispatched |
| 2026-09-17 | "Batch" replaced by a real, orderable "Stage" catalog (Clean Out/Pest Control/Wall-Paint Prep/Paint/General/Plumbing/Electrical/HVAC/Exterior/Landscaping/Final Cleaning), managed at Set Up → Stages; Job Timeline now shows one row per Stage, not per line item or per batch | User request. Replaces the old unused free-text `quote_stage` column (see 2026-09-10 log, "never populated by anything") with a real fixed catalog — `stages` table with a `sort_order` reviewers control from Set Up, driving dropdown/summary order everywhere (Quote Sheet, Stage View, Job Timeline). `batch_number` stays as the internal PropertyWare-work-order grouping key (unchanged, still paused pending real PW API access) since one vendor can still need separate batches split across stages — `createBatches` now groups by (Stage, Assigned To/Vendor) instead of just (Assigned To/Vendor). Stage scheduling/status lives in a new `inspection_stages` table (one row per inspection+stage, mirroring the per-line-item scheduling columns) rather than aggregating line items, so a Stage's schedule/blocked-by has one clear source of truth independent of how many items or batches fall under it. Reorder UI (▲/▼ swapping `sort_order` with a neighbor) is a new precedent — no drag library, matching the app's existing no-new-JS-dependencies posture; Stage labels render as plain text/borders, never condition or status colors, per the 2026-09-11 violation already on record |
| 2026-09-17 | Fixed: Stage reorder arrows silently no-op'd on every click | Root cause: `sort_order` is `unique`, and the swap wrote the neighbor's value directly onto the current row while the neighbor still held that same value — an immediate unique-constraint violation. Fixed by routing the swap through a temporary out-of-range value (-1) inside one transaction |
| 2026-09-17 | Fixed: Tenant Charge amount rejected any value with cents | `step="1"` on a dollar input meant a labor-hours-derived total like $161.25 failed HTML5 step validation. Changed to `step="0.01"` — a plain, unrestricted dollar-and-cents entry |
| 2026-09-17 | Added Take-Off Sheet: a per-batch PDF (`/inspections/[id]/quote-sheet/stages/[batchNumber]/pdf`), one button per batch on Stage View | User request: a worker/vendor scope-of-work sheet per Stage assignment, headed by who it's for and when it's scheduled — distinct from the owner-facing Quote Sheet PDF. Deliberately carries no $ amounts (Room/Item/Comments/Supplier-SKU only) since it goes to whoever is doing the work, not the owner. Header resolves "For" (vendor name / GPM Staff / "Mixed assignment") and "Stage" the same deduped way Stage View's own `assigneeLabel`/`stageLabel` do, rather than trusting one arbitrary row — a batch isn't guaranteed uniform since `batch_number` stays manually reassignable. Scheduled date is read from `inspection_stages` (the Stage's own schedule), not stored per batch |
| 2026-09-18 | Added standalone "Tenant Chargeback Review" screen (`/inspections/[id]/chargeback-review`), replacing "Create Move Out Report" as the main inspection page's entry point; the Tenant Charge checkbox/amount was removed from the main inspection page's big line-item table entirely | User (Jessica) feedback: the tenant-charge decision (which items are chargebacks, at what $ amount) is a senior-PM judgment call against a hard 30-day security-deposit disposition deadline (MCL 554.608), and often has to happen before the Quote Sheet's materials/labor/vendor/stage data exists — burying it in the same table as those AI-extraction/quoting fields made a fast pass slower than it needed to be. New screen shows only Item/Condition (read-only, via a new `ConditionBadge` using DESIGN.md's 4 reserved condition colors) plus the charge decision, with a running "N items · $total" strip and "Generate Move-Out Report" as the very next action on the same page. The amount field starts blank (not pre-filled with materials+labor cost, unlike the old inline control) — user confirmed this number and the eventual rehab quote are independent and don't need to reconcile. New `updateChargebackReview` action touches only `tenant_charge`/`tenant_charge_amount`, so it can't be clobbered by an unrelated save on the main page; `bulkUpdateLineItems` no longer writes those two columns. Old `TenantChargeCheckboxes` component deleted (fully superseded) |
| 2026-09-18 | Job Timeline replaced by a portfolio-wide "Dispatch Board" (`/dispatch-board`, new top-level nav item), showing every active job's Stages at once instead of one job's Gantt | User feedback: the per-inspection Gantt "was not functional for managing multiple, sometimes 8-10 jobs at a time." Reviewed a working prototype first (an Artifact sketch) before building. One grid, two groupings toggled live — By Property (rows = job) and By Crew (rows = GPM Staff / each vendor, answering "who's free next week") — both reading the same `inspection_stages` data, not two separate features. An Unscheduled Stages tray holds any Stage-with-line-items that has no `inspection_stages` row yet or null dates; dragging a chip onto a date schedules it via a new `updateStagePlacement` action, an upsert on `(inspection_id, stage_id)`. Dragging a scheduled bar onto a different crew's row bulk-rewrites `assigned_to`/`vendor_id` on every line item in that Stage — a deliberate act (this vendor now owns this Stage), not an incidental side effect; dropping on a different property's row in Property grouping is a no-op, since a Stage can't be moved to a different job. Old per-inspection `/inspections/[id]/timeline` route, `JobTimelineView`, and the now-unused `updateStageSchedule` action deleted (fully superseded); Quote Sheet's "Job Timeline" button became "Dispatch Board", linking to `/dispatch-board?job=<id>` |
| 2026-09-18 | Dispatch Board: bar edges are drag-to-resize (pointer events, same technique as the original Gantt's drag-to-reschedule); drop targets in Property grouping are restricted to a Stage's own job (invalid rows show the browser's native "not allowed" cursor instead of silently no-op'ing); the 14-day window became a user-selectable 15/20/30-day range | User requests. Duration previously had no real logic (a flat 2-day default with no way to change it) — resize replaces that guess with direct manual control rather than trying to auto-derive "days" from item count or labor hours, which would need assumptions about crew size the app doesn't have. The day-range control needed `grid-template-columns` to become dynamic per render, which a static Tailwind arbitrary-value class can't express — switched those three grid containers to inline `style` |
| 2026-09-18 | **Reopened and reversed** the 2026-09-11 decision against color-coding assignee type: added `vendor` (a new, real second accent — a muted slate-blue, `#3D6B8A`/`#E9F1F6`) to color Outside Vendor cards on the Dispatch Board differently from GPM Staff cards | User decision, made explicitly aware of the prior reversal (inventing a "non-palette blue" for the same Good/Damaged taxonomy was caught and reverted during the original Timeline's design review) before choosing to reopen it anyway. Scoped deliberately narrow: `vendor` exists to solve one problem (telling crew types apart at a glance across many jobs on one board) and is not licensed for use anywhere else in the app without its own decision. GPM Staff cards keep the plain neutral card treatment every other card in the app already uses; only Vendor cards take the new color. Chosen hex avoids both the reserved condition colors and GPM's brand-cyan/brand-indigo (logo-only) hues |
| 2026-09-18 | Fixed: Move-Out Report's "Itemized Charges to Tenant" still showed Materials/Labor columns and an "hours @ rate" line pulled from `materials_cost`/`labor_cost`/`labor_hours`, and its Total fell back to materials+labor whenever no override existed | User report: the table looked like it was pricing off stale Quote Sheet cost fields instead of the amount entered on Tenant Chargeback Review, even though the Total column itself already preferred `tenant_charge_amount` when set — the real problem was that fallback (and the now-orphaned Materials/Labor columns sitting next to it) still assuming the two numbers were related, exactly the coupling that screen was built to remove. Table is now Item / Room-Area / Amount Charged, sourced from `tenant_charge_amount` only; a charged item with no amount entered yet reads "Not yet entered" and is excluded from the grand total (not silently priced from an unrelated field, and not silently $0.00) |
| 2026-09-18 | Tenant Chargeback Review gets two top-of-page actions: "Review Move-Out Report" (plain link, opens the PDF to check before committing to anything) and "Generate and Post Report" (new `PostMoveOutReportButton`, confirmed via browser `confirm()` like `DeleteInspectionButton`) | User request. "Generate and Post Report" is a STAND-IN for the real PropertyWare integration (same posture as `sendBatchToPW`/Stage View) — there's no PW API access/docs yet to actually attach the PDF to the tenant's file, so the reviewer posts it in PW themselves and records the resulting reference here (`inspections.move_out_report_posted_at`/`move_out_report_pw_reference`, migration `0021`), which still gives a real audit trail. Calls the server action directly from `onSubmit` rather than a plain form `action` so it can open the PDF once posting completes — the new tab is opened synchronously before the `await` (redirected afterward) since browsers frequently block `window.open()` calls made after an async gap, even a short one |
| 2026-09-18 | Take-Off Sheet PDF: Supplier/SKU column replaced with a blank ruled "Notes" line | User request. Not a data field — a blank line per item for the crew to write on in the field (materials picked up, something found, anything worth flagging back), same paper-form convention as the signature lines on the Move-Out Report. `supplier`/`sku` dropped from this route's query entirely |
| 2026-09-18 | Attempted fix (insufficient): Dispatch Board drag-and-drop wasn't dropping into the calendar in either grouping | First pass added `dataTransfer.setData(...)` to `onDragStart` and disabled pointer-events on bars mid-drag, on the theory that native HTML5 DnD just needed those two gaps closed. User reported it was still broken after this shipped — see next entry for the actual fix |
| 2026-09-18 | **Real fix**: replaced native HTML5 drag/drop entirely with pointer events for the Dispatch Board's whole-item drag (reschedule / Crew reassignment) | The previous entry's native-DnD patches didn't resolve it, and this environment has no way to drive a real browser interactively to debug native DnD's usual failure modes further — rather than guess a third native-DnD fix, switched to the mechanism already proven working in this exact app (the resize handles built alongside this feature, and the original per-job Gantt before Dispatch Board replaced it): `onPointerDown` + window-level `pointermove`/`pointerup`, with `document.elementsFromPoint()` (plural — returns every element at a point in stacking order, not just the topmost) used to find the day-cell under the cursor even when a bar is visually on top of it. This sidesteps both suspected native-DnD root causes at once instead of patching around them. **Verified for real**, not just by inspection: installed `playwright-core` temporarily (not a project dependency, removed after) to drive this machine's actual installed Chrome, performed a real mouse-down/move/up drag against the running dev server, and confirmed via direct DB query that `updateStagePlacement` actually ran — both the Unscheduled-tray-to-calendar case and the Crew-view cross-row reassignment case (`assigned_to`/`vendor_id` rewritten, date shifted) were confirmed this way, with screenshots of the resulting board state |
| 2026-09-18 | Quote Sheet's "Stages" summary strip is now a set of jump links to that stage's rows | User request. The table itself still isn't grouped by Stage (stays ordered by `room_area`, same as every other row-editing page in this app) — each stage's link jumps to the first row carrying that stage in that same order, same anchor technique (a `firstRowIdByX` map + a conditional `id`) the main inspection page already uses for its room-area quick-jump links |
| 2026-09-18 | Added a "Recent" section to the nav rail (new `RecentNav`, per-device via `localStorage` — this app has no auth/session concept) | User request, for quick navigation back to whatever was just open. Uses `useSyncExternalStore` to read `localStorage` reactively rather than `useEffect` + `setState` (a real lint error, not a style nit — that pairing causes a render cascade); recording the current visit is a genuine side effect and stays in a plain `useEffect`, which only writes and dispatches a change event rather than touching component state itself. Entries are keyed off whatever each page already passes as AppShell's own `title`, which meant fixing three inspection-scoped pages (Quote Sheet, Stage View, Tenant Chargeback Review) whose titles were generic ("Quote Sheet") and would have collided across different jobs in this list — they now include the property address, and Dispatch Board's title includes it too when filtered to one job via `?job=` |
| 2026-09-18 | Quote Sheet gets the same "Remove Section" (staged hard delete, resolved on Save) / "Duplicate" controls the main inspection page already has, bottom-right of each row | User request. `updateQuoteSheetItems` now checks `remove__<id>` the same way `bulkUpdateLineItems` already did; `duplicateLineItem` is shared between both pages, so its own `revalidatePath` now covers the Quote Sheet route too. Duplicating now also copies `stage_id`/`supplier`/`sku` (previously silently dropped) since a duplicate on this page usually means "the same repair needs a second instance" — same Stage and materials source as a starting point — while `batch_number` stays deliberately un-copied, same reasoning as the scheduling fields already omitted here: a duplicate is a newly-noticed task, not yet dispatched |
| 2026-09-18 | Added Additional SKUs: a line item can now carry more than one Supplier/SKU pair (new `line_item_additional_skus` table) | User request: a repair sometimes needs more than one part. The existing `supplier`/`sku` columns stay the primary entry; additional ones are their own rows, added/removed immediately (`addLineItemSku`/`removeLineItemSku`) rather than folded into `updateQuoteSheetItems`' bulk save — the count of these per item is dynamic, and parsing a variable-length list back out of one big form's `FormData` is real complexity a plain insert-now button avoids entirely, same reasoning as `duplicateLineItem` already being an immediate action in this same form |
| 2026-09-18 | Added SKU quantities (`line_items.sku_quantity`, `line_item_additional_skus.quantity`) and a job-level "Bulk Materials" list (new `inspection_bulk_materials` table) for purchases shared across many line items | User request: (1) a per-item quantity next to each SKU, and (2) a real recurring situation — buying one contractor pack of outlets (or a roll of window screen, a tube of caulk) and using it across 8 different repair line items in the same house. Recording that against one line item would misrepresent a single purchase as several; a many-to-many link to every row it covers was considered and explicitly passed over (user decision) in favor of a plain job-level list, not tied to any specific line item — Supplier/SKU/Quantity/Notes, added/removed immediately like Additional SKUs. Both quantity columns are `text`, not numeric — quantities here range from plain counts ("2") to units a number can't hold ("50 ft", "1 roll"), and forcing a numeric field would just push reviewers to fudge the shape |
| 2026-09-18 | Added Materials Order List PDF (`/inspections/[id]/quote-sheet/materials-order/pdf`), a "Materials Order List" button on Quote Sheet | User request: a shopping list generated from the Quote Sheet, aggregating every SKU recorded across all three sources (a line item's primary Supplier/SKU, its Additional SKUs, and every Bulk Materials entry) into one PDF grouped by Supplier — so whoever does the buying knows what to grab at each store, in one pass instead of reading every line item's fine print. Distinct from the Quote Sheet PDF (owner-facing, carries $ amounts) and the Take-Off Sheet (per-batch scope of work, no SKUs at all): this one exists purely to support purchasing. Verified against real data spanning all three sources at once, grouped correctly under a shared Supplier |
| 2026-09-20 | Fixed: the status pill anywhere in the app didn't visually update until the page was left and revisited, even though the status change saved immediately | Root cause: `StatusSelect` used an uncontrolled `<select defaultValue>` inside its own `<form>`, and computed the pill's background/text color from the `status` PROP -- which only changes when the parent Server Component re-fetches on a real navigation. Submitting the form saved the new value to the database right away, but nothing told this client component to re-render with it. Fixed by making it a controlled select with local state, updated optimistically the instant a value is picked and calling the server action directly (no `<form>` needed) rather than waiting on a page revalidation to reach it. Verified with a real browser: picked a new status, confirmed the pill's computed background color changed within under a second with no page reload |
| 2026-09-20 | Reworked the inspection detail page's header: `AppShell` gained an optional `headerContent` prop (replaces the default title/"Reviewing as" row entirely when given, every other page unaffected) so this page could show "Property: X · Job # · Inspector: Y · Date" as one line with no "Reviewing as"; the row below is now Status pill + Tenant Chargeback Review + Quote and Schedule (renamed from "Create Quote Sheet") left-aligned, with Delete Inspection pushed to the far right | User request. Once status reaches Approved (same `QUOTE_ACTIONS_VISIBLE_STATUSES` gate the Quote Sheet page already uses for its own copies), the same row also shows Dispatch Board / Create Batches / Stage View / Materials Order List / Create Quote, so a reviewer doesn't have to leave this page once those are live. Verified against real data in both states (status flipped to `under_review` and back) that the gated buttons appear/disappear correctly |
| 2026-09-20 | `StatusSelect` pill gets a real border (color derived from its own text color, same derivation `StatusBadge` already uses for its dot) | User request: without one, a colored pill with no border read as a plain label, not a control someone could click to change. Applies everywhere this component is used (inspection detail, Quote Sheet), not just the one page that prompted it |
| 2026-09-20 | Added "View Image Folder" (new `/inspections/[id]/stills` gallery, grouped by room, bordered frame + mono caption per photo) next to "Source videos (Drive)"; moved Video Processing's toggle/action button onto that same links row | User request: a way to review every still frame extracted from an inspection's video in one place, instead of one hover-preview (`EvidenceStill`) at a time from the line-item table. No GPS data exists in this schema for the caption DESIGN.md's evidence-frame convention describes, so it carries Room/Item instead — the identifying info that does exist. `VideoProcessingPanel` now returns a Fragment instead of its own bordered block, with `basis-full` forcing its expanded detail (progress bar, video list) onto its own line within the shared `flex-wrap` row rather than needing a separate container. Verified against real data: gallery rendered 56 real extracted stills grouped correctly by room |
| 2026-09-20 | Quote Sheet: renamed "Create Batches" → "Create Stages" button; Bulk Materials' "×" remove button enlarged and made solid red (was small/gray, only red on hover); Remove Section/Duplicate controls moved from bottom-right to bottom-left of each line item; Supplier/SKU pulled out of the cramped 9th grid column into its own full-width horizontal row beneath each item's main fields | User request: the Supplier/SKU column was "very hard to navigate and view" stacked vertically in a narrow track alongside 8 other columns. `ROW_COLS` dropped from 9 to 8 tracks; the new Supplier/SKU row is a `col-span-full` grid child, which CSS Grid auto-placement naturally pushes onto its own line since it can't fit in the row above |
| 2026-09-20 | Added a bulk-item auto-fill suggestion (design doc: `docs/designs/quote-sheet-bulk-item-auto-fill.md`) — adding a Bulk Material now surfaces a confirm banner listing line items it looks like it covers, with Apply/Dismiss, instead of a silent write | User request, scoped down from a much larger "Auto Process Quote" idea (vendor assignment, labor-hour estimates, Cost Book pricing, continual learning) via /office-hours — those need historical data that doesn't exist yet and are deferred. v1 matches on the Bulk Material's SKU field only (not supplier or notes — verified against real data that supplier is a generic store name and notes is noisy freeform text, both of which broke matching entirely on the first attempt), with basic trailing-`s` stemming, requiring ALL significant words to appear in the item name, and only ever considers line items with blank Supplier/SKU — apply never overwrites existing reviewer data. Verified end-to-end with a real browser against the actual 11020 Brookwood inspection: banner correctly matched exactly the 2 real "Attic Light Bulb" rows for a real "Light Bulbs" bulk item, Apply wrote Supplier/SKU/Qty to both, and Dismiss on a synthetic test bulk item left line items untouched |
| 2026-09-20 | Removed "Reviewing as {name}" from every page's header (`AppShell`'s `reviewerName` prop deleted entirely, not just hidden) | User request. No real session/user model exists yet (see `app/login/page.tsx`), so the line was always a static placeholder, never a real "who's logged in" indicator |
| 2026-09-20 | Added "Back to Inspection" button to the Quote Sheet page's toolbar | User request, for parity with the Image Folder and Tenant Chargeback Review pages, which already had one |
| 2026-09-20 | Fixed two real bugs found via a user bug report on real data: (1) the bulk-item auto-fill was copying the bulk material's `quantity` (the size of the whole purchase, e.g. a 6-pack) onto each matched line item's `sku_quantity` — now only Supplier/SKU are copied, never quantity, since a line item's own comment states how many IT needs; (2) `duplicateLineItem`'s explicit column list omitted `sku_quantity` (added in migration 0023, after that function was last written), silently dropping it on every duplicate | Investigated after the user reported "you just duplicated the 2 light bulb inspection items and add 6 to the quantity." Root cause was two separate things compounding: the wrong-quantity bug, plus 2 genuine accidental duplicate rows created by clicking Duplicate (confirmed via `created_at`/`updated_at` timestamps) — both real rows deleted and the wrong quantity cleared on the real 11020 Brookwood inspection with user confirmation before deleting |
| 2026-09-20 | Quote Sheet: Bulk Materials list restructured into per-item bordered cards labeled "Material Item 1", "Material Item 2", etc., each with Edit (inline form) and Delete (renamed from "×") controls; Remove Section/Duplicate moved out of `absolute` bottom-corner positioning into a normal-flow row directly above the Supplier/SKU row | User request: Edit didn't exist before (only remove), the list had no per-item labeling, and Remove Section/Duplicate "looked disconnected" from the item once Supplier/SKU became its own row beneath the main fields. `updateBulkMaterial` resets `matches_reviewed = false` on save so the auto-fill banner re-evaluates against corrected data |
| 2026-09-20 | Added "Share Images" to the Image Folder page: check a subset of stills, then "Create Image Library Link" generates an unauthenticated, tokenized public URL (`/share/[token]`) and copies it to the clipboard | User request. New `image_share_links` table stores a JSONB snapshot of the selected images (url/room/item) at link-creation time, not a live join to `line_items` — a shared link keeps showing exactly what was shared even if those line items are later edited or removed from the Quote Sheet. No auth model exists in this app yet (see `app/login/page.tsx`), so the token itself is the only access control, matching how the still images' underlying Supabase Storage URLs already work. Verified end-to-end with a real browser: selected 2 real photos, link generated and copied, public page rendered the correct property address and both images |
| 2026-09-20 | Quote Sheet per-item row reworked: fixed a Bulk Materials Edit/Delete vertical-alignment bug (the `<form>` wrapping Delete added a box the plain `<a>` Edit link didn't have — fixed with `display: contents` on the form); Materials $ and Labor (hrs) moved out of the main row into the "Materials for this Item" row (now centered, was "Supplier/SKU"), alongside a new "Total: $X.XX" next to Remove Section/Duplicate (self-performed: materials + labor×rate; outside vendor: vendor quote); renamed "Add" → "Add Item"; added a "Use Bulk Item" dropdown (lists the job's Bulk Materials) that copies a bulk item's Supplier/SKU onto the line item and clears its `materials_cost` — that item's material cost now lives on the bulk item's own new `cost` column (migration 0026) instead, since it's covered by a shared purchase, not a separate per-item cost; `labor_hours` is untouched | User request. Split `LineItemAssignment` into two components since it's shared with the inspection detail page's own (unrelated, unchanged) line-item table: `LineItemVendorAssignment` (Quote Sheet's slimmer main-row cell) and `LineItemMaterialsCost` (the relocated Materials $/Labor pair, own copy of the self-performed check from the same initial value — a small, accepted risk of going briefly out of sync with the main row's assignment dropdown until the row is saved). Verified end-to-end against real data: linked "Toilet Base Caulking" (a real line item) to the real "Caulk - White" bulk item, confirmed materials_cost cleared to null while labor_hours (0.25) stayed, then reverted |
| 2026-09-20 | Inspection detail page: added an "Inspection Report" label before the property address; moved the Status pill to the far right of the top header line (`AppShell`'s `headerContent` now returns two flex children so its `justify-between` has something to space apart); removed Assigned To/Materials/Labor (hrs)/Vendor Est. entirely from this page's own line-item table (buttons on the second line shift left as a natural consequence of Status no longer being there) | User request: cost assignment now happens exclusively on the Quote Sheet page. Deleted the now-fully-unused `LineItemAssignment.tsx` component (its only remaining caller). Critical fix alongside the UI change: `bulkUpdateLineItems` unconditionally wrote `assigned_to`/`materials_cost`/`labor_hours`/`labor_cost`/`vendor_estimated_cost`/`vendor_id` from form fields on every save — with the inputs removed but the SET clauses left in place, every save from this page would have silently nulled those columns on every line item. Fixed by dropping those columns from the action's SET clause entirely, not just removing the inputs |
| 2026-09-20 | Quote Sheet: removed the redundant `= $X.XX` / `@ $rate/hr` line under the Labor (hrs) input (the "Total: $X.XX" added earlier this session already covers it); converted the "Add Item" mini-form to a client component (`AddLineItemSku.tsx`) | User request (redundant total) plus a second, repeated "Add Item does nothing" report. The underlying write was verified working via direct database checks both times it was reported -- the real problem was silent-looking success: with the old plain `<form>`/`formAction` version, React only applies `defaultValue` on mount, so after the server action's `revalidatePath` round trip the reviewer's just-typed text stayed sitting in the box with no visible change except a new chip elsewhere in a long flex-wrap row, indistinguishable from nothing happening. The client version uses controlled inputs that actually clear on success and a button that shows "Adding…", making success visible instead of assumed |
| 2026-09-20 | Removed "Send to PW" (the manually-typed PW work order number stand-in) and its resulting "PW WO#" confirmation from the Stage View page | User request: not pursuing that workflow right now. `app/actions.ts`'s `sendBatchToPW` and the `work_order_batches` table are left in place, not deleted -- explicitly a "not now" deferral, not a decision to abandon the feature |
| 2026-09-20 | Renamed Quote Sheet's "Materials for this Item" to "Materials for This Section"; `AddLineItemSku` now catches and displays a visible error instead of silently swallowing one | User request, plus a THIRD "Add Item does nothing" report despite the write verifiably succeeding via direct database checks on the two prior reports. Real root cause still unconfirmed -- most likely a stale page open across one of this session's several redeploys (Next.js Server Actions are keyed per build; a stale reference fails silently). The visible error is a diagnostic net: if it fires next time, the exact failure reason becomes visible instead of assumed |
| 2026-09-20 | Renamed to "Materials for Above Section"; redesigned the section so it always shows exactly ONE item box by default (the line item's primary Supplier/SKU/Qty, plus a "Use Bulk Item" picker beside it) instead of that box PLUS a permanently-visible blank "add new" mini-form -- the real cause of the user's "it looks like 2 items" report. "Add Item" is now a single button that, when clicked, reveals one full-size blank box (matching the primary box exactly, not the smaller style used before) with its own freehand-or-bulk-item choice; saving it collapses the box back to just the button, ready to click again for a third item. New `addLineItemSkuFromBulkMaterial` action mirrors `linkLineItemToBulkMaterial` but inserts a new additional-SKU row instead of overwriting the line item's primary fields | User request: "I want ONE item as a default with the ability to select from a bulk item or enter free hand... the add item button would duplicate the original blank item boxes... does not need to be any more difficult than that." Verified end-to-end with a real browser: default state shows one box only, clicking Add Item reveals a matching full-size blank box, saving it produces a real database row and the box collapses back to the button |
